//-------------
					1.主页面和注销用的是同一个页面。
					2.初始密码在login.jsp这个页面修改。
					4.验证码的有效时间为两分钟。
					3.登陆的验证码,是被放在session里面的,
							它的名字是那个Constants类里面一个变量的值,
					
							
					5.登陆的账号密码 那些东西,都通过日志被记录到了日志文件里面
					
					
					6	登陆的action如果收到后面有异常,则会在ModelAndView中设置一个名message,
						的变量,页面会去取这个变量 ,并显示异常信息
					7.判断的顺序是
							验证码过期
							验证码不正确
							账号是否存在,
							密码是否正确
							是否被冻结
					8.验证通过之后,
						在session中存入了
						用户id
						用户账号
						验证码:	(之前的)
					9.注销
						首先清空了数据库表中数据
						然后清空session
								用户账号
								用户id
								
					10.所有登陆和验证都完成之后,
					进入sysadmin的时候,里面的index.jsp
								(1).才会用到inc.jsp页面(动态包涵)
									inc.jsp中引入了所有的model
										路径
										css文件夹内容
										js
																	
								(2).载入主框架
							
					11.sysadmin中的frame是主要框架
					12.登陆成功后里面的主页面的位置在
						sysadmin/welcome.html
	
						
13.一个登陆用户左边的树出现哪些菜单是由数据库中的数据决定的,

14.如果没登陆直接去访问某个页面,是由过滤器决定的
	这个就是过滤是否登陆,
	没登陆,的时候给一个alert报错,这个alert是在CheckValidAdminFilter类里面拼接出来的
15.有时候登陆了,但是这个员工没权限访问这个页面,这个就叫权限过滤

16.一个用户登陆了,是否有权访问这个url也是由数据库里面数据决定的
17。checkAuth另外检证权限的时候超级管理员是可以直接访问的，不需要验证权限，这个动作是在过滤器中处理的

18. 先看是否需要验证(.js加入到了不需要验证的行列，)
	如果验证，有些只验证是否登陆
	剩下的全部要验证权限(这里目前发现需要验证的都是.do)
	

					//-------------
					
					